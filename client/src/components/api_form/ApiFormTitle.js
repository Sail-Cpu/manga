import React from "react";

const ApiFormTitle = (props) => {
    return(
        <div className="api-form-title">
            <h1>{props.name}</h1>
        </div>
    )
}

export default ApiFormTitle;